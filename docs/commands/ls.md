# `ls`

Print the tree view of an image.

## Synopsis

```bash
mkpfs ls --image ./output.ffpfs
```

## Notes

This command reuses the image parser and then renders the filesystem tree from the parsed structure.
