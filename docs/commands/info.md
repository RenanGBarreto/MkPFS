# `info`

Show lightweight metadata about a PFS image such as header fields, size and basic version info.

## Synopsis

```bash
mkpfs info --image ./output.ffpfs
```

## Notes

This command is intended to be quick and safe for scripts. It prints key header values and overall image statistics.
